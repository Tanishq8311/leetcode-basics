bool flag=0;
    for(int i=0;i<S.size()/2;i++){
        if(S[i]==S[S.size()-i-1])
            flag=1;
        else 
            flag=0;
    }
    if(flag==1)
        return 1;
    else 
        return 0;
